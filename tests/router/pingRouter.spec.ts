import { expect, describe, it } from "vitest";

describe("manipulating database", () => {
	it("should get the user email and password", () => {
		const certo = 2;
		expect(certo).toEqual(2);
	});
	it("should get the user email and password", () => {
		const certo = 2;
		expect(certo).toEqual(2);
	});
});
